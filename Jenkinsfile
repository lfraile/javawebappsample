pipeline {
    agent { node { label 'dotnet' } } 
    stages {
        stage('Example Build') {
            steps {
                sh 'echo hola'
                //bat 'terraform version'
            }
        }        
    }
}
