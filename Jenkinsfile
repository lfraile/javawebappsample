pipeline {
    agent { node { label 'dotnet' } } 
    stages {
        stage('Example Build') {
            steps {
                bat 'echo hola'
                //bat 'terraform version'
            }
        }        
    }
}
